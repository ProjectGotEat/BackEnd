<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="javaeatsong.goteat.repository.MessagesMapper">
    <insert id="insert">
        INSERT INTO messages(participant_id, sender_id, receiver_id, content)
        VALUES (#{pid}, #{uid}, #{receiverId}, #{content});
    </insert>
</mapper>