<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="javaeatsong.goteat.repository.ReportsMapper">

	<insert id="insert"
		parameterType="javaeatsong.goteat.model.Reports">
		INSERT INTO reports(participant_id, reporter_id, reportee_id, category_id, content)
		VALUES (#{participantId}, #{reporterId}, #{reporteeId}, #{categoryId}, #{content})
	</insert>
	
</mapper>